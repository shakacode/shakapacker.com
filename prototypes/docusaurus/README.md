# Docusaurus Site

This folder contains the production Docusaurus app for `shakapacker.com`.

## Commands

- `npm run start`
- `npm run build`
- `npm run serve`

Docs content in `./docs` is generated from monorepo sources by running at repo root:

- `npm run sync:docs`
- `npm run prepare:docs`
